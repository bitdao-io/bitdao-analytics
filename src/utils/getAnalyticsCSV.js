const getAnalyticsAPIJSON = require('./getAnalyticsAPIJSON');

const headers = [
  
]

function getAnalyticsCSV(conf, conns) {
    json = getAnalyticsAPIJSON(conf, conns);
}

module.exports = getAnalyticsAPIJSON;
